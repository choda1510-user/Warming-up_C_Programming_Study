#include <stdio.h>

int main(int argc, char *argv[])
{
    char size_char;
    printf("clothing size(S,M,L)?");
    scanf("%c", &size_char);
    printf("choose %c size.\n", size_char);
    return 0;
}

