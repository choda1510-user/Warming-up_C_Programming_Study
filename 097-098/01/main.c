#include <stdio.h>

int main(int argc, char *argv[])
{
    int length;
    printf("length? ");
    scanf("%d", &length);
    printf("squrare area: %d\n", length * length);
    printf("squrare round: %d\n", length * 4);
    return 0;
}

